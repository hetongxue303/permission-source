server:
  port: 8080
spring:
  application:
    name: permission
  config:
    import:
      - application-web.yml
      - application-log.yml
      - application-mybatisPlus.yml
      - application-datasource.yml
      - application-redis.yml